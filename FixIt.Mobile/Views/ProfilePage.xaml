<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="FixIt.Mobile.Views.ProfilePage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FixIt.Mobile.Localization"
             xmlns:models="clr-namespace:FixIt.Mobile.Models"
             xmlns:viewModels="clr-namespace:FixIt.Mobile.ViewModels"
             xmlns:views="clr-namespace:FixIt.Mobile.Views"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             x:DataType="viewModels:ProfileViewModel"
             BackgroundColor="{StaticResource Background}"
             NavigationPage.HasNavigationBar="False"
             ios:Page.UseSafeArea="True"
             Title="{local:Localize Key=Profile_Title}">

    <ScrollView>
        <VerticalStackLayout x:Name="ProfileRoot"
                             Padding="20,28,20,40"
                             Spacing="16"
                             Opacity="0"
                             TranslationY="25">

            <!-- Identity Card -->
            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="12" HorizontalOptions="Center">
                    <Border WidthRequest="88" HeightRequest="88"
                            BackgroundColor="{StaticResource Primary}"
                            StrokeThickness="0"
                            StrokeShape="RoundRectangle 44"
                            HorizontalOptions="Center">
                        <Border.Shadow>
                            <Shadow Brush="{StaticResource Primary}"
                                    Opacity="0.4" Offset="0,4" Radius="14" />
                        </Border.Shadow>
                        <Label Text="{Binding Initials}"
                               TextColor="{StaticResource White}"
                               FontSize="Title"
                               FontAttributes="Bold"
                               HorizontalOptions="Center"
                               VerticalOptions="Center" />
                    </Border>

                    <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
                        <Label Text="{Binding DisplayName}"
                               Style="{StaticResource HeadlineMedium}"
                               HorizontalTextAlignment="Center"
                               SemanticProperties.HeadingLevel="Level1" />
                        <Label Text="{Binding Email}"
                               Style="{StaticResource LabelSmall}"
                               HorizontalTextAlignment="Center" />
                    </VerticalStackLayout>

                    <Border BackgroundColor="{StaticResource StatusNew}"
                            StrokeThickness="0"
                            Padding="12,5"
                            StrokeShape="RoundRectangle 10"
                            HorizontalOptions="Center">
                        <Label Text="{Binding TrustLevel, StringFormat='TRUST · {0}'}"
                               FontSize="Caption"
                               FontAttributes="Bold"
                               CharacterSpacing="1"
                               TextColor="{StaticResource White}" />
                    </Border>
                </VerticalStackLayout>
            </Border>

            <!-- Language Switcher -->
            <Border Style="{StaticResource Card}">
                <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
                    <Label Grid.Column="0"
                           Text="{local:Localize Key=Profile_Language}"
                           Style="{StaticResource TitleMedium}"
                           VerticalOptions="Center"
                           SemanticProperties.HeadingLevel="Level2" />
                    <Button Grid.Column="1" Text="EN"
                            Command="{Binding SwitchLanguageCommand}"
                            CommandParameter="en-US"
                            Style="{StaticResource SecondaryButton}"
                            HeightRequest="42" WidthRequest="56">
                        <Button.Triggers>
                            <DataTrigger TargetType="Button" Binding="{Binding IsEnglish}" Value="True">
                                <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                <Setter Property="TextColor" Value="{StaticResource White}" />
                                <Setter Property="BorderColor" Value="{StaticResource Primary}" />
                            </DataTrigger>
                        </Button.Triggers>
                    </Button>
                    <Button Grid.Column="2" Text="BG"
                            Command="{Binding SwitchLanguageCommand}"
                            CommandParameter="bg-BG"
                            Style="{StaticResource SecondaryButton}"
                            HeightRequest="42" WidthRequest="56">
                        <Button.Triggers>
                            <DataTrigger TargetType="Button" Binding="{Binding IsBulgarian}" Value="True">
                                <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                <Setter Property="TextColor" Value="{StaticResource White}" />
                                <Setter Property="BorderColor" Value="{StaticResource Primary}" />
                            </DataTrigger>
                        </Button.Triggers>
                    </Button>
                </Grid>
            </Border>

            <!-- Stats Row -->
            <Grid ColumnDefinitions="*,*,*" ColumnSpacing="12">
                <Border Grid.Column="0" Style="{StaticResource StatCard}">
                    <VerticalStackLayout Spacing="6">
                        <Label Style="{StaticResource LabelSmall}"
                               Text="{local:Localize Key=Profile_Reputation}" />
                        <Label FontSize="Title" FontAttributes="Bold"
                               TextColor="{StaticResource Accent}"
                               Text="{Binding ReputationPoints}" />
                    </VerticalStackLayout>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource StatCard}">
                    <VerticalStackLayout Spacing="6">
                        <Label Style="{StaticResource LabelSmall}"
                               Text="{local:Localize Key=Profile_Reported}" />
                        <Label FontSize="Title" FontAttributes="Bold"
                               TextColor="{StaticResource OnBackground}"
                               Text="{Binding IssuesReported}" />
                    </VerticalStackLayout>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource StatCard}">
                    <VerticalStackLayout Spacing="6">
                        <Label Style="{StaticResource LabelSmall}"
                               Text="{local:Localize Key=Profile_Resolved}" />
                        <Label FontSize="Title" FontAttributes="Bold"
                               TextColor="{StaticResource Success}"
                               Text="{Binding IssuesResolved}" />
                    </VerticalStackLayout>
                </Border>
            </Grid>

            <!-- My Issues -->
            <VerticalStackLayout Spacing="14">
                <Grid ColumnDefinitions="*,Auto">
                    <Label Grid.Column="0"
                           Style="{StaticResource TitleMedium}"
                           Text="{local:Localize Key=Profile_MyIssues}"
                           SemanticProperties.HeadingLevel="Level2" />
                    <Label Grid.Column="1"
                           Text="{local:Localize Key=Profile_ViewAll}"
                           TextColor="{StaticResource Accent}"
                           FontSize="Caption"
                           FontAttributes="Bold"
                           VerticalOptions="Center">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding ViewAllIssuesCommand}" />
                        </Label.GestureRecognizers>
                    </Label>
                </Grid>

                <CollectionView ItemsSource="{Binding MyIssues}"
                                SelectionMode="None"
                                VerticalOptions="Start">
                    <CollectionView.EmptyView>
                        <VerticalStackLayout Spacing="0">
                            <VerticalStackLayout IsVisible="{Binding IsLoading}" Spacing="0">
                                <views:SkeletonCardView />
                                <views:SkeletonCardView />
                                <views:SkeletonCardView />
                            </VerticalStackLayout>
                            <Label Text="{local:Localize Key=Profile_NoIssues}"
                                   IsVisible="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                                   Style="{StaticResource BodyMedium}"
                                   HorizontalTextAlignment="Center"
                                   Padding="0,24" />
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Issue">
                            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                                <Grid RowDefinitions="Auto,Auto" RowSpacing="8">
                                    <Grid Grid.Row="0" ColumnDefinitions="*,Auto" ColumnSpacing="10">
                                        <Label Grid.Column="0"
                                               Style="{StaticResource TitleMedium}"
                                               Text="{Binding Title}"
                                               MaxLines="1"
                                               LineBreakMode="TailTruncation" />
                                        <Border Grid.Column="1"
                                                StrokeThickness="0"
                                                StrokeShape="RoundRectangle 8"
                                                Padding="8,4"
                                                VerticalOptions="Center"
                                                BackgroundColor="{Binding Status, Converter={StaticResource StatusToColorConverter}}">
                                            <Label Text="{Binding Status}"
                                                   TextColor="{StaticResource White}"
                                                   FontSize="Caption"
                                                   FontAttributes="Bold" />
                                        </Border>
                                    </Grid>
                                    <HorizontalStackLayout Grid.Row="1" Spacing="4">
                                        <Label Text="📍" FontSize="Caption" />
                                        <Label Text="{Binding CityName}" Style="{StaticResource LabelSmall}" />
                                        <Label Text="·" Style="{StaticResource LabelSmall}" />
                                        <Label Text="{Binding CreatedAt, Converter={StaticResource DateTimeToRelativeConverter}}"
                                               Style="{StaticResource LabelSmall}" />
                                    </HorizontalStackLayout>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>

            <!-- Sign Out -->
            <Button Text="{local:Localize Key=Profile_SignOut}"
                    Style="{StaticResource SecondaryButton}"
                    TextColor="{StaticResource Error}"
                    BorderColor="{StaticResource Error}"
                    Command="{Binding LogoutCommand}"
                    Margin="0,8,0,0"
                    SemanticProperties.Description="Sign out of your account" />

            <ActivityIndicator IsRunning="{Binding IsLoading}"
                               IsVisible="{Binding IsLoading}"
                               Color="{StaticResource Primary}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>